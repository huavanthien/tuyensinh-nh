import type { Application, SchoolClass, Announcement, Guideline } from './types';
import { ApplicationStatus, EnrollmentType, EnrollmentRoute } from './types';

// Mock data has been moved to the backend (db.json).
// This file is kept for shared enums and types if needed in the future.
